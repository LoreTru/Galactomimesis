/* =============================================================================
   version.js — unica fonte del numero di versione

   Cambiare la versione qui, in un solo posto. I commenti di intestazione
   negli altri file (index.html, main.js) riportano solo il nome del
   progetto, non il numero di versione — per evitare che il numero scritto
   lì si disallinei da questo nel tempo (non c'è un passo di build che li
   terrebbe sincronizzati automaticamente).
   ============================================================================= */
export const GM_VER = '0.6';
